# SPEC — 디스플레이 시장 이슈 조기 감지

- 문서 상태: 초안
- 작성일: 2026-08-13
- 근거 문서: `BRIEF.md`(전체 구상), `PLAN.md`(6~8시간 실행 계약)
- 기술 스택: Next.js + JSON 파일 (DB 없음)

이 문서는 `BRIEF.md`의 요약본이 아니다. **이번에 실제로 만들 것**의 명세이며,
각 절은 구현자가 되묻지 않아도 되는 수준까지 값을 박아 둔다.

---

## 1. 범위 경계

`BRIEF.md`는 언젠가 만들 전체를, `PLAN.md`는 내일 발표 전까지 만들 것을 적은 문서다.
이 SPEC은 그 사이를 다음과 같이 가른다.

| 구분 | 내용 | 이 문서에서의 취급 |
|---|---|---|
| **반드시 (Phase 1~4)** | 샘플 데이터 + 로더 / 지표 계산 / 추세·분포 차트 / 기간·제품 필터 | 명세 + 이번에 구현 |
| **후속 (Phase 5)** | 급증 탐지 — 전일·전주 대비 증가율 | 명세만. 구현은 반드시 범위 이후 |
| **후속 (Phase 6)** | 규칙 기반 제품·카테고리·감성 분류 | 명세만. 구현은 반드시 범위 이후 |
| **후속 (Phase 7)** | 인증 없는 공개 RSS 출처 1개 연결 | 명세만. 구현은 반드시 범위 이후 |
| **범위 밖** | 제품 관리 화면 (5.6절) | `data/products.json` 직접 편집으로 대체 |
| **범위 밖** | 수집 상태 화면 (6절) | Phase 7 이후에나 의미가 있다 |
| **범위 밖** | 이슈 탐색 별도 화면 (6절) | 대시보드 하단 원문 목록으로 흡수 |
| **범위 밖** | 정기 수집 스케줄링, Vercel 배포, 인증·권한, 이메일·Slack 알림 | `PLAN.md` ③에서 "안 하는 것"으로 확정 |
| **범위 밖** | 분류 결과 사용자 수정 (5.3절), 번역, 동영상 댓글 | 후순위 |

### 1.1 저장소를 SQLite가 아니라 JSON 파일로 두는 이유

`BRIEF.md` 8절은 SQLite를 "필수"로 적었으나 이 SPEC은 따르지 않는다.
이번 범위에는 **수집도 누적도 없다.** 고정된 샘플 데이터를 읽기만 하므로 DB가 하는 일
(동시 쓰기, 중복 방지, 인덱스)이 하나도 발생하지 않는다. 스키마·마이그레이션 세팅에
드는 30~60분은 6~8시간에서 적지 않고, 샘플 데이터를 눈으로 고치는 일도 JSON이 빠르다.

**나중에 바꿔 끼울 때 건드릴 곳**: 7절의 로더 인터페이스 `loadFeedback()` 하나뿐이다.
집계·필터·화면은 로더가 무엇을 읽는지 모른다. SQLite나 PostgreSQL로 가더라도
이 함수의 구현만 갈면 된다.

---

## 2. 도메인 값 집합

> **이 절은 `PLAN.md` ⑤의 승인 지점이다.** 여기가 틀리면 샘플 데이터부터 차트까지 전부 틀어진다.
> 구현 시작 전에 이 절을 먼저 확정받는다.

### 2.1 제품 (`products.json`, 3개)

실제 제품명·브랜드명은 쓰지 않는다. 전부 지어낸 값이다.

| `id` | `category` (제품군) | `brand` | `name` | `is_active` |
|---|---|---|---|---|
| `P-001` | 스마트폰 | 노바텍 | 루멘 폰 X | true |
| `P-002` | 태블릿 | 노바텍 | 아틀라스 탭 11 | true |
| `P-003` | 모니터 | 오리온디스플레이 | 클리어뷰 모니터 27 | true |

### 2.2 열거형

| 이름 | 값 |
|---|---|
| 제품군 `product_category` | `스마트폰` · `태블릿` · `노트북` · `모니터` · `TV` |
| 이슈 카테고리 `issue_category` | `화질` · `성능` · `소비전력` · `가격` · `내구성` · `발열` · `기타` (7개, `BRIEF.md` 5.2절 그대로) |
| 세부 이슈 `issue_subcategory` | 색 균일도 · 번인 · 플리커 · 밝기 · 색 정확도 · 화면 잔상 · 응답 속도 · 배터리 소모 · 기타 |
| 감성 `sentiment` | `positive` · `negative` · `neutral` |
| 출처 유형 `source_type` | `news` · `community` · `video` |
| 분석 방법 `analysis_method` | `seed`(샘플에 미리 채움) · `rule`(Phase 6) · `llm`(범위 밖) |
| 언어 `language` | `ko` 고정 — 이번 범위는 한국어만 다룬다 |

이슈 카테고리는 7개를 그대로 쓴다. 분포 차트의 조각 수가 곧 7이고, 이것이 8절의 검증 기대값이 된다.

---

## 3. 데이터 모델

`BRIEF.md` 7절을 이번 범위로 좁힌 것이다. **원문과 분석을 분리해 저장**한다(`BRIEF.md` 5.3절).
파일은 세 개, 위치는 `data/`.

### 3.1 `data/products.json` — `Product[]`

| 필드 | 타입 | 필수 | 비고 |
|---|---|---|---|
| `id` | string | ✓ | `P-001` 형식 |
| `category` | 제품군 열거형 | ✓ | |
| `brand` | string | ✓ | |
| `name` | string | ✓ | |
| `aliases` | string[] | | 이번 범위에서는 화면에 안 쓰임. Phase 6 분류기의 입력 |
| `search_keywords` | string[] | | 동일 |
| `exclude_keywords` | string[] | | 동일 |
| `is_active` | boolean | ✓ | false면 필터 목록과 차트에서 제외 |
| `created_at` / `updated_at` | ISO 8601 | ✓ | |

이 파일을 손으로 고쳐 제품을 늘릴 수 있다 — 코드 수정 없이 모니터링 대상을 추가한다는
`BRIEF.md` 핵심 성공 기준을, 관리 화면 없이 이 방식으로 충족한다.

### 3.2 `data/feedback_items.json` — `FeedbackItem[]` (300건)

| 필드 | 타입 | 필수 | 값 범위·형식 |
|---|---|---|---|
| `id` | string | ✓ | `FI-0001` ~ `FI-0300` |
| `source_id` | string | ✓ | `S-01`~`S-05` (4.4절) |
| `external_id` | string | ✓ | 출처 내 고유값. 지어낸 값 |
| `title` | string | ✓ | 한국어 12~40자 |
| `content_excerpt` | string | ✓ | 한국어 60~240자 |
| `original_url` | string | ✓ | `https://example.com/...` 형식의 **가짜 URL**. 실제 사이트를 가리키지 않는다 |
| `author_name` | string | ✓ | 비식별 값 고정 — `익명1` ~ `익명40` |
| `published_at` | ISO 8601 | ✓ | 2026-07-15 ~ 2026-08-13 |
| `collected_at` | ISO 8601 | ✓ | `published_at` + 0~2일 |
| `language` | `"ko"` | ✓ | 고정 |
| `content_hash` | string | ✓ | 중복 판별용. 이번 범위에서는 값만 들어 있고 쓰이지 않는다 |

### 3.3 `data/feedback_analyses.json` — `FeedbackAnalysis[]` (300건, 1:1)

| 필드 | 타입 | 필수 | 값 범위·형식 |
|---|---|---|---|
| `id` | string | ✓ | `FA-0001` ~ `FA-0300` |
| `feedback_item_id` | string | ✓ | `FeedbackItem.id`와 1:1 |
| `product_id` | string | ✓ | `P-001`~`P-003` |
| `issue_category` | 열거형 | ✓ | 7개 중 하나 |
| `issue_subcategory` | 열거형 | ✓ | |
| `sentiment` | 열거형 | ✓ | |
| `summary` | string | ✓ | **샘플에 미리 채워 둔다.** 한국어 40~100자 |
| `keywords` | string[] | ✓ | 2~5개 |
| `confidence_score` | number | ✓ | 0.60 ~ 0.98 |
| `analysis_method` | `"seed"` | ✓ | 이번 범위는 전부 `seed` |
| `analyzed_at` | ISO 8601 | ✓ | |

`summary`를 샘플에 미리 채우는 이유: **앱 실행에 API 키가 한 개도 필요 없게 하기 위해서다.**
화면은 이 값을 표시만 하고, 표시할 때 "참고용 분석 결과"임을 명시한다(5.6절).
나중에 LLM 키가 생기면 `analysis_method`를 `llm`으로 바꾸고 이 필드를 채우는 쪽만 갈아끼우면 된다.

### 3.4 이번엔 파일을 만들지 않는 모델

`Source`와 `CollectionRun`은 Phase 7(수집)에서 필요하다. 스키마는 `BRIEF.md` 7절을 그대로 따르되,
이번 범위에서는 4.4절의 출처 5개를 상수로 두고 파일은 만들지 않는다.

---

## 4. 샘플 데이터 명세

검증이 가능하도록 분포를 숫자로 고정한다. **아래 표의 숫자가 곧 8절 테스트의 기대값이다.**

### 4.1 총량과 기간

| 항목 | 값 |
|---|---|
| 총 건수 | **300건** |
| 기간 | **30일** — 2026-07-15 ~ 2026-08-13 (기준일 `2026-08-13` 고정) |
| 제품 수 | **3개** |
| 이슈 카테고리 | **7개** |
| 언어 | 한국어만 |

기준일을 고정값으로 박는다. "오늘"을 실행 시각에서 읽으면 며칠 뒤에 테스트가 깨진다.

### 4.2 제품별 분포

| 제품 | 총 건수 | 부정 | 중립 | 긍정 |
|---|---|---|---|---|
| `P-001` 루멘 폰 X | 140 | 77 | 45 | 18 |
| `P-002` 아틀라스 탭 11 | 90 | 36 | 33 | 21 |
| `P-003` 클리어뷰 모니터 27 | 70 | 22 | 27 | 21 |
| **합계** | **300** | **135** | **105** | **60** |

전체 부정 반응 비율 = 135 / 300 = **45.0%**

### 4.3 카테고리별 분포

| 카테고리 | 건수 |
|---|---|
| 화질 | 96 |
| 성능 | 54 |
| 발열 | 39 |
| 소비전력 | 33 |
| 내구성 | 30 |
| 가격 | 27 |
| 기타 | 21 |
| **합계** | **300** |

7개 카테고리 모두 건수가 0보다 크다 — 분포 차트에 조각이 반드시 7개 나온다.

### 4.4 출처

| `id` | `name` | `source_type` |
|---|---|---|
| `S-01` | 가상 IT 뉴스 A | `news` |
| `S-02` | 가상 IT 뉴스 B | `news` |
| `S-03` | 가상 사용자 커뮤니티 A | `community` |
| `S-04` | 가상 사용자 커뮤니티 B | `community` |
| `S-05` | 가상 영상 채널 | `video` |

### 4.5 심어 둘 급증 구간

`P-001`에 최근 7일 부정 반응 급증을 **일부러 심는다.** Phase 5(급증 탐지)의 입력이자,
발표 때 "이 제품이 이번 주에 튀었다"를 보여 줄 소재다.

| 구간 | 날짜 | 전체 건수 | `P-001` 건수 | `P-001` 부정 건수 |
|---|---|---|---|---|
| 최근 7일 | 2026-08-07 ~ 08-13 | 111 | 63 | 45 |
| 직전 7일 | 2026-07-31 ~ 08-06 | 63 | 26 | 12 |
| 그 이전 16일 | 2026-07-15 ~ 07-30 | 126 | 51 | 20 |
| **합계** | | **300** | **140** | **77** |

`P-001`의 부정 반응 전주 대비 증가율 = (45 − 12) / 12 = **+275.0%**
급증 구간의 이슈는 `화질` / `색 균일도`에 몰아 둔다 — 디스플레이 개발자가 볼 이야기여야 한다.

### 4.6 생성 규칙

- **말투** — 한국어 커뮤니티 게시물체. 뉴스 출처(`S-01`,`S-02`)는 기사 제목체, 커뮤니티
  출처(`S-03`,`S-04`)는 구어체, 영상 출처(`S-05`)는 영상 제목체로 구분되게 쓴다.
  `PLAN.md` ⑤의 "샘플 데이터가 그럴듯한가"는 사람이 눈으로 볼 항목이며, 이 규칙이 그 기준이다.
- **날짜 분포** — 4.5절 구간 합계를 지키되 하루 단위로는 들쭉날쭉하게 둔다. 30일 모두 최소 1건 이상.
- **감성과 카테고리** — 4.2·4.3절 합계를 정확히 맞춘다.

### 4.7 금지 규칙

- 실제 제품명·브랜드명·모델명을 쓰지 않는다. 2.1절의 가상 이름만 쓴다.
- 실제 사이트 URL을 쓰지 않는다. `https://example.com/...` 형식의 가짜 URL만 쓴다.
- 실명·이메일·전화번호 등 개인정보를 쓰지 않는다. 작성자는 `익명N` 고정.
- 사내 실데이터·고객명·시스템 계정·API 키를 코드와 데이터 어디에도 넣지 않는다.

---

## 5. 화면 명세

**대시보드 한 화면**이다. 별도 라우트를 만들지 않는다. 위에서 아래로 다섯 영역.

### 5.1 필터 바 (상단 고정)

| 컨트롤 | 값 | 기본값 |
|---|---|---|
| 기간 | 오늘 / 최근 7일 / 최근 30일 | **최근 30일** |
| 제품 | 전체 / `P-001` / `P-002` / `P-003` | **전체** |

- `is_active`가 false인 제품은 목록에 나오지 않는다.
- 두 필터는 **동시에 적용**된다(AND).
- 아래 네 영역 전부가 같은 필터 결과를 본다.

### 5.2 지표 카드 (2개)

| 카드 | 표시 값 | 읽는 필드 |
|---|---|---|
| **언급 건수** | 필터 적용 후 건수 | `FeedbackItem.published_at` |
| **부정 반응 비율** | 필터 적용 후 negative 비율, 소수점 첫째 자리 | `FeedbackAnalysis.sentiment` |

`BRIEF.md` 6절의 원래 이름은 "신규 수집 건수"였다. **1단계에는 수집이 없으므로 그 이름을 쓰지 않는다.**
카드가 실제로 세는 것은 필터 조건에 걸린 게시물의 수이고, 그것을 6.2절이 "언급량"으로 정의한다.
수집이 붙는 Phase 7 이후에 "오늘 새로 들어온 건수"를 따로 보여 주고 싶어지면 그때 카드를 하나 더 만든다 —
같은 카드의 뜻을 바꾸지 않는다.

각 카드 우측에 **급증 배지 자리**를 비워 둔다. Phase 5에서 `+275.0%` 같은 값이 들어올 자리이며,
이번 범위에서는 렌더하지 않는다. 자리를 미리 잡아 두는 이유는 Phase 5에서 레이아웃을 다시 짜지 않기 위해서다.

### 5.3 날짜별 언급량·감성 추세 그래프

- x축: 날짜 (기간 필터가 정한 일수만큼의 눈금)
- y축: 건수
- 계열 3개: 긍정 / 부정 / 중립 — 색과 범례로 구분
- 읽는 필드: `FeedbackItem.published_at`, `FeedbackAnalysis.sentiment`

### 5.4 카테고리별 분포 차트

- 이슈 카테고리 7개의 건수 비중
- 읽는 필드: `FeedbackAnalysis.issue_category`
- 건수 0인 카테고리도 범례에는 남긴다 (필터 결과에 따라 0이 될 수 있다)

### 5.5 원문 목록 (하단)

`BRIEF.md`의 핵심 성공 기준인 **원문 추적성**을 이 영역이 담당한다.
별도 화면을 만들지 않고 대시보드 하단에 붙인다.

| 열 | 읽는 필드 |
|---|---|
| 제목 | `FeedbackItem.title` — 클릭하면 `original_url`을 새 탭으로 |
| 제품 | `FeedbackAnalysis.product_id` → 제품명 |
| 카테고리 | `FeedbackAnalysis.issue_category` |
| 감성 | `FeedbackAnalysis.sentiment` — 색으로 구분 |
| 출처 | `FeedbackItem.source_id` → 출처명, `source_type` 뱃지 |
| 작성일 | `FeedbackItem.published_at` |
| 요약 | `FeedbackAnalysis.summary` |

- 정렬 기본값: **최신순** (`published_at` 내림차순)
- 한 번에 20건, 더 보기로 이어 받는다
- 필터를 걸면 이 목록의 건수도 함께 바뀐다 — 8절 Phase 4 검증이 이 건수를 본다

### 5.6 요약 표시 규칙

`summary`를 보여 줄 때는 **"참고용 분석 결과"** 임을 함께 표시하고, 같은 행에서 원문 링크로
갈 수 있어야 한다(`BRIEF.md` 5.7절 · 11절 설명 가능성). 사실로 확정된 문장처럼 보이게 두지 않는다.

### 5.7 빈 상태와 오류 상태

| 상황 | 화면 |
|---|---|
| 필터 결과 0건 | 차트 영역에 "이 조건에 해당하는 데이터가 없습니다" — 빈 차트 축만 그리지 않는다 |
| 데이터 파일 없음·JSON 파싱 실패 | 화면 전체에 오류 메시지와 원인 파일명 |
| 스키마 검증 실패 | 어느 레코드의 어느 필드가 틀렸는지 표시 |

---

## 6. 계산 규칙

기준일은 `2026-08-13` 고정이다(4.1절).

### 6.1 기간 필터

| 값 | 범위 |
|---|---|
| 오늘 | `published_at`이 2026-08-13 |
| 최근 7일 | 2026-08-07 ~ 2026-08-13 (기준일 포함 7일) |
| 최근 30일 | 2026-07-15 ~ 2026-08-13 (기준일 포함 30일) |

경계는 **양끝 포함**이다.

### 6.2 언급량

```
언급량(기간, 제품) = 조건을 만족하는 FeedbackItem의 개수
```

### 6.3 부정 반응 비율

```
부정 반응 비율 = (sentiment == "negative" 건수) / (전체 건수) × 100
```

- 전체 건수가 0이면 비율을 계산하지 않고 화면에 `—` 를 표시한다. 0%로 쓰지 않는다.
- 소수점 첫째 자리까지, 반올림.

### 6.4 증가율 — Phase 5, 이번엔 구현하지 않는다

```
증가율 = (이번 구간 건수 − 직전 구간 건수) / 직전 구간 건수 × 100
```

- 직전 구간은 이번 구간과 **같은 길이**로 바로 앞에 붙인다.
  최근 7일(08-07~08-13)의 직전 구간은 07-31~08-06이다.
- 직전 구간 건수가 0이면 백분율을 만들지 않는다. `신규` 로 표시한다 — 0으로 나누지 않기 위해서다.
- 급증 배지 임계값: **+50% 이상이고 이번 구간 건수가 10건 이상**일 때만 표시한다.
  건수가 작을 때의 큰 백분율은 신호가 아니라 잡음이다.
- 4.5절 기준 `P-001` 부정 반응은 +275.0%로 이 조건을 만족한다.

---

## 7. 모듈 경계

집계·필터·화면은 데이터가 **어디서 오는지 모른다.** 로더 뒤에 숨긴다.
분류기(Phase 6)와 수집기(Phase 7)가 끼어들 자리를 지금 뚫어 둔다.

```ts
// 로더 — 저장소를 바꿀 때 여기만 갈아끼운다 (1.1절)
type FeedbackRecord = FeedbackItem & { analysis: FeedbackAnalysis };
function loadProducts(): Product[];
function loadFeedback(): FeedbackRecord[];

// 필터
type FilterState = { period: "today" | "7d" | "30d"; productId: string | "all" };
function applyFilter(records: FeedbackRecord[], filter: FilterState): FeedbackRecord[];

// 집계
function countTotal(records: FeedbackRecord[]): number;
function negativeRatio(records: FeedbackRecord[]): number | null;   // 0건이면 null (6.3절)
function trendBySentiment(records: FeedbackRecord[], from: string, to: string)
  : { date: string; positive: number; negative: number; neutral: number }[];
function distributionByCategory(records: FeedbackRecord[])
  : { category: IssueCategory; count: number }[];                   // 항상 7개 원소

// Phase 5 — 명세만
function growthRate(current: FeedbackRecord[], previous: FeedbackRecord[]): number | "new";

// Phase 6 — 분류가 안 붙은 원문을 받아 분석을 만든다
function classify(item: FeedbackItem, products: Product[]): FeedbackAnalysis;

// Phase 7 — 공개 RSS 출처 하나를 FeedbackItem 배열로 바꾼다
function fetchFromSource(source: Source): Promise<FeedbackItem[]>;
```

- `trendBySentiment`는 데이터가 없는 날짜도 0으로 채워 반환한다. 그래야 x축 눈금 수가 기간과 일치한다.
- `distributionByCategory`는 건수가 0인 카테고리도 원소로 남긴다. 항상 길이 7이다.

---

## 8. 검증 명세

`PLAN.md` ④의 완료 문장을 **기계가 판정할 수 있는 기대값**으로 번역한 것이다.
숫자는 전부 4절 샘플 데이터 분포에서 나온다.

### 8.1 Phase별 완료 조건

| Phase | 완료 조건 | 판정 수단 |
|---|---|---|
| 1. 샘플 데이터 + 로더 | 스키마 검증을 통과하고, `loadFeedback()`이 **정확히 300건**을 반환한다. `loadProducts()`가 3건을 반환한다. 모든 `FeedbackAnalysis.feedback_item_id`가 실재하는 `FeedbackItem.id`와 1:1로 대응한다 | 단위 테스트 |
| 2. 지표 계산 | 8.2절 기대값 표의 모든 행이 일치한다 | 단위 테스트 |
| 3. 추세·분포 차트 | 페이지를 열면 추세 그래프의 **x축 눈금이 30개**, 분포 차트의 **조각이 7개** 렌더된다 | 브라우저 자동 검사 |
| 4. 기간·제품 필터 | 8.3절 필터 조합 표의 원문 목록 건수가 전부 일치한다 | 브라우저 자동 검사 |

### 8.2 Phase 2 기대값 — 지표 계산

| 조건 | 건수 | 부정 반응 비율 |
|---|---|---|
| 전체 (최근 30일 · 전체 제품) | 300 | 45.0% |
| `P-001` · 최근 30일 | 140 | 55.0% |
| `P-002` · 최근 30일 | 90 | 40.0% |
| `P-003` · 최근 30일 | 70 | 31.4% |
| 전체 · 최근 7일 | 111 | — (8.4절) |
| `P-001` · 최근 7일 | 63 | 71.4% |

카테고리 분포는 4.3절 표(화질 96 · 성능 54 · 발열 39 · 소비전력 33 · 내구성 30 · 가격 27 · 기타 21)와 일치해야 한다.

### 8.3 Phase 4 기대값 — 필터 조합별 원문 목록 건수

| 기간 | 제품 | 기대 건수 |
|---|---|---|
| 최근 30일 | 전체 | 300 |
| 최근 30일 | `P-001` | 140 |
| 최근 30일 | `P-002` | 90 |
| 최근 30일 | `P-003` | 70 |
| 최근 7일 | 전체 | 111 |
| 최근 7일 | `P-001` | 63 |

"줄어든 값으로 바뀐다"가 아니라 **정확한 숫자**로 판정한다. 필터가 엉뚱한 필드를 걸러도
건수는 줄어들 수 있기 때문이다.

### 8.4 채워야 할 값

`전체 · 최근 7일`의 부정 반응 비율은 4.5절이 구간별 총 건수와 `P-001` 부정 건수만 고정하고
`P-002`·`P-003`의 구간별 감성 분포를 아직 나누지 않았기 때문에 지금 확정할 수 없다.
**샘플 데이터를 생성하는 Phase 1에서 실제 분포를 확정한 뒤 이 표에 값을 적어 넣는다.**
그 전까지는 이 행을 테스트에서 제외한다 — 임의의 숫자를 지금 박으면 검증이 아니라 짜맞추기가 된다.

### 8.5 사람이 눈으로 볼 것

기계 판정과 섞지 않는다. 통과·실패로 적지 않고, 봤는지 여부만 남긴다.

- 그래프가 읽힐 만한가 — 축·범례·색이 구분되고 30일 추세가 한눈에 보이는가.
  요소가 렌더되는 것과 읽히는 것은 다르다.
- 샘플 데이터가 그럴듯한가 — 출처 유형별 말투가 구분되는가, 이슈 분포가 실제 커뮤니티 글처럼 보이는가.
- 급증이 눈에 띄는가 — `P-001`을 최근 7일로 필터했을 때 부정 반응이 튀는 것이 보이는가.

---

## 9. 비기능 요구

`BRIEF.md` 11절 중 이번 범위에 해당하는 것만 남긴다.

| 항목 | 이번 범위에서 |
|---|---|
| 원문 추적성 | 5.5절 원문 목록의 모든 행에서 `original_url`로 이동할 수 있다 |
| 설명 가능성 | 5.6절 — `summary`는 "참고용 분석 결과" 표시와 원문 링크를 함께 낸다 |
| 확장성 | 제품은 `products.json` 편집으로 추가한다. 출처·저장소는 7절 인터페이스로 갈아끼운다 |
| 보안 | API 키를 쓰지 않는다. 소스와 데이터 어디에도 키가 없다 |
| 개인정보 보호 | 작성자는 `익명N` 고정. 실명·연락처를 만들지 않는다 |
| 법적 준수 | 이번 범위에는 수집이 없다. 실제 사이트에 요청을 보내지 않으므로 약관·robots.txt 문제가 발생하지 않는다 |
| 데이터 신뢰성 | 해당 없음 — 수집이 없어 중복·수집 오류가 발생하지 않는다. Phase 7에서 다시 본다 |
| 수정 가능성 | 해당 없음 — 분류 결과 사용자 수정은 범위 밖(1절) |

---

## 10. 열린 결정

### 10.1 구현 전에 승인이 필요한 것

1. **2절 도메인 값 집합** — 제품 3개의 가상 이름, 이슈 카테고리 7개, 세부 이슈 목록.
   `PLAN.md` ⑤가 지정한 승인 지점이다.
2. **5절 화면 레이아웃** — 다섯 영역의 순서와 각 영역의 구성.
   `PLAN.md` ②는 "승인된 레이아웃대로 구현한다"고 적었으므로 승인이 선행해야 한다.
3. **기술 스택 확정** — Next.js + JSON은 정해졌으나 차트 라이브러리는 아직 없다.
   되돌리기가 비싼 결정이므로 첫 커밋 전에 정한다.

### 10.2 Phase 5~7로 미룬 것

| Phase | 내용 | 이 문서에 있는 것 |
|---|---|---|
| 5 | 급증 탐지 | 계산 규칙(6.4절), 화면 자리(5.2절), 심어 둔 급증 구간(4.5절) |
| 6 | 규칙 기반 분류 | 인터페이스(7절), 입력이 될 `Product.search_keywords`·`exclude_keywords`(3.1절) |
| 7 | 공개 RSS 출처 1개 | 인터페이스(7절), `Source`·`CollectionRun` 스키마(3.4절) |

### 10.3 이 문서로도 답이 안 나오는 것

- 8.4절 — `전체 · 최근 7일`의 부정 반응 비율. Phase 1에서 확정한다.
- 앱 안에서의 실제 LLM 호출. 사내 API 키 발급이 제한적이라는 제약이 있어 이번 범위 밖에 두었다.
  `analysis_method` 필드가 그 자리를 표시하고 있다(3.3절).
