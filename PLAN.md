# 디스플레이 시장 이슈 조기 감지

**Capstone 트랙**: 트랙 A — 풀스택 웹앱
**구현에 쓸 수 있는 시간**: 코칭 전후·저녁 + 다음 날 발표 전까지 (실질 6~8시간)
**기술 스택**: Next.js + JSON 파일 (DB 없음)

## ① 문제 정의 — 지금 무엇이 불편한가
- 누가 / 언제 / 어떤 일에서: 모바일·IT 제품용 디스플레이 개발자가, 시장에서 화질 이슈가 발생한 뒤, 소비자 커뮤니티와 뉴스에서 제품 관련 반응을 직접 찾아보는 일에서. 지금은 커뮤니티 → 뉴스 → 경영진 인지 → 조사 지시 순서로 전달되기 때문에 개발자가 초기 신호를 놓치고 이슈가 커진 뒤 급하게 대응하게 된다. 이를 방지 하기 위해서는 담당자를 두고 게시물이나 유튜브 등을 주기적으로 모니터링 해야 한다.
- 줄이는 비용 (반복 / 대기 / 검증 중 하나): **반복** — 매번 손으로 같은 순서를 밟아 제품별로 커뮤니티·뉴스를 검색해 본다.
- 지금 걸리는 시간·횟수: 주 2~3회 / 회당 30분~1시간
- 이번 6~8시간이 실제로 줄이는 것: **아직 없다.** 가상 데이터를 읽는 대시보드라 실제 검색 반복은 한 번도 줄지 않는다. 이번에 증명하는 것은 "실제 데이터가 들어왔을 때 이 화면이 이슈를 보여 줄 수 있는가"까지이고, 반복이 실제로 줄어드는 것은 첫 실제 출처가 붙는 Phase 7부터다. 발표에서 이 경계를 그대로 말한다.

## ② AI 에이전트에게 맡길 부분
- 에이전트가 대신할 일: 승인된 스키마대로 가상 게시물 데이터 300건 생성(요약 `summary`까지 채워서) / 승인된 레이아웃대로 지표 카드·그래프·분포 차트·필터·원문 목록 구현 / 각 Phase의 "완료:"를 판정할 단위 테스트와 브라우저 자동 검사 스크립트 작성 / Phase를 순서대로 진행하며 매번 검증을 돌리고 통과한 뒤에만 다음으로 넘어가기
- 사람이 계속 할 일: 샘플 데이터 스키마와 도메인 값 집합 확정(어떤 필드를 받을지, 제품 3개와 이슈 카테고리 7개를 무엇으로 할지) / 화면 레이아웃 승인 / 차트 라이브러리 선택 승인 / 그래프가 읽힐 만한지 보기 / 샘플 데이터가 실제 커뮤니티 글처럼 그럴듯한지 보기 / 반드시 범위가 끝난 뒤 Phase 5로 넘어갈지 판단
- 쓸 도구 (서브에이전트·MCP·스킬·hook·병렬 중 필요한 것만):
  - **CLAUDE.md 하네스** — 샘플 데이터 스키마·폴더 구조·금지 사항(실데이터 금지, API 키 금지)을 적어 두고 매 Phase마다 같은 규칙으로 일하게 한다.
  - **스킬 — 샘플 데이터 생성기** — 같은 스키마로 가상 게시물을 계속 찍어내는 절차를 스킬로 만들어 재사용한다. 데이터가 반드시 범위의 1번 Phase이자 승인 지점이라 절차를 고정할 값어치가 있다.
  - **hook — 저장 시 테스트·타입체크** — 파일이 바뀔 때마다 자동 검사를 돌려, 깨진 상태로 다음 Phase에 넘어가지 않게 한다.
  - (선택 안 함) 서브에이전트 — 대시보드 1개 범위라 나눠 돌릴 만큼 일이 갈라지지 않는다.

## ③ 범위 — 반드시 / 되면 좋은 / 안 하는 것
- 반드시 (이게 안 되면 실패): 가상(샘플) 데이터 300건으로 채운 **대시보드 화면 1개**. 위에서 아래로 다섯 영역 — 기간·제품 필터 바 / 지표 카드 2개(언급 건수, 부정 반응 비율) / 날짜별 언급량·감성 추세 그래프 / 카테고리별 분포 차트 / 원문 목록(제목을 누르면 원문 링크로 이동). 데이터는 제품 3개 · 30일치 · 이슈 카테고리 7개 · 한국어.
- 되면 좋은: 반드시 범위가 3시간 안에 끝나면 순서대로 이어 붙인다. **Phase 5** 급증 탐지(전주 대비 증가율 배지) → **Phase 6** 규칙 기반 제품·카테고리·감성 분류 → **Phase 7** 인증 없는 공개 RSS 출처 1개 연결.
- 이번엔 안 하는 것: **앱 안에서의 LLM 호출** — 사내 API 키 발급이 제한적이라, 앱 실행에 키가 한 개도 필요 없게 만드는 쪽을 택했다. 요약은 샘플 데이터에 미리 채워 두고 화면은 표시만 한다. 그 밖에 정기 수집 스케줄링(cron·GitHub Actions), Vercel 배포, 로그인·권한, 이메일·Slack 알림, 제품 관리·수집 상태 화면.

## ④ Phase 분할과 각 Phase 완료 기준
분할 축: 아래에서 위로 — 데이터 → 지표 → 차트 → 필터

기준일은 고정값으로 박는다. "오늘"을 실행 시각에서 읽으면 며칠 뒤에 완료 조건이 저절로 깨진다.

1. 샘플 데이터 + 로더 — 완료: 스키마 검증을 통과하고, 명령 한 번으로 **정확히 300건**의 게시물과 **3건**의 제품이 로드된다. 300건의 분석 레코드가 전부 실재하는 원문과 1:1로 대응한다. **이 Phase에서 제품별·감성별·카테고리별·기간별 건수 표를 확정해 고정한다** — 뒤 Phase의 기대값이 전부 이 표에서 나온다
2. 지표 계산 (언급 건수·부정 반응 비율) — 완료: 계산 함수의 단위 테스트가 1번에서 고정한 건수 표의 값과 **전부 일치**한다. 최소한 전체 / 제품 3개 각각 / 최근 7일 조합에 대한 테스트가 있다
3. 추세·분포 차트 — 완료: 페이지를 열면 추세 그래프의 **x축 눈금이 30개**, 분포 차트의 **조각이 7개** 렌더된다는 것을 브라우저 자동 검사가 통과한다
4. 기간·제품 필터 — 완료: 필터 조합 6가지(기간 3종과 제품 전체·개별을 엇갈린 것)의 원문 목록 건수가 1번에서 고정한 건수 표의 값과 **정확한 숫자로** 일치한다

> Phase 5~7(급증 탐지 / 규칙 기반 분류 / 공개 RSS 1개)등은. 시간이 남으면 이어서 한다.

## ⑤ 완료를 판정할 방법 (검증 게이트)
- 기계가 판정하는 것: 단위 테스트 통과 / 명령 한 번으로 샘플 데이터 JSON이 생성되고 건수가 찍히는 스크립트 출력물 / Playwright 등으로 페이지를 열어 차트 눈금 수·조각 수와 필터별 목록 건수를 자동 검사. 기대값은 전부 Phase 1에서 고정한 건수 표에서 가져온다.
- 사람이 눈으로 보는 것: 그래프가 읽힐 만한가(축·범례·색이 구분되고 30일 추세가 한눈에 보이는가) / 샘플 데이터가 그럴듯한가(출처 유형별 말투가 구분되고 이슈 분포가 실제 커뮤니티 글처럼 보이는가) / 급증을 심어 둔 제품을 최근 7일로 필터했을 때 부정 반응이 튀는 것이 눈에 띄는가
- 내가 직접 승인할 지점:
  1. **도메인 값 집합** — 어떤 필드를 받을지, 제품 3개와 이슈 카테고리 7개를 무엇으로 할지. 여기가 틀리면 샘플 데이터부터 차트까지 전부 틀어진다.
  2. **화면 레이아웃** — 대시보드 다섯 영역의 순서와 구성. 승인 없이 구현하면 다시 짜야 한다.
  3. **차트 라이브러리 선택** — 되돌리기가 가장 비싼 결정이라 첫 커밋 전에 정한다.

## GitHub 저장소 URL
https://github.com/minyup/test

## 1:1 코칭에서 가장 묻고 싶은 것
1) 대시보드 완성 후 API 를 붙여 요약하는 과정에 API 키가 필요할텐데 사내에서 API 키 발급 제한적, 다른 방법 없는지
2) 어제 BRIEF.md 를 만들어 봤고, 모델별로 SPEC.md 파일을 만들어 봤는데 수준이 다다름

```
 c:\minyup_work\개인과제\BRIEF.md 를 참고해서 SPEC.md 초안을 제시해줘. 단, 모호하거나 서로 충돌하는 요구가 있으면 네가 임의로 정하지 말고 먼저 질문해줘.
  질문은 한 번에 모아서, 선택지와 각 선택의 결과를 붙여서 물어봐.
  내가 답하기 전에는 SPEC.md 파일을 만들지 마.
```

 품질이나 형식이 다양한데 어떤 SPEC 문서가 좋은 SPEC인지?
 유사한 수준으로 답변되게 하려면 어떤 정보들을 더 넣어야 하는지?

BRIEF.md: https://app.notion.com/p/BRIEF-md-3ba0a6db3ade80d1b2a0f9b4f7af427d?source=copy_link
 ├ SPEC_Opus: https://app.notion.com/p/SPEC_opus-3ba0a6db3ade8081892bc9c748e74e02?source=copy_link
 ├ SPEC_Sonet: https://app.notion.com/p/SPEC_sonet-3ba0a6db3ade80cb8a79ef80b037186d?source=copy_link
 └ SPEC_5.6_Sol_middle: https://app.notion.com/p/SPEC_5-6_sol-3ba0a6db3ade80208565f9397914c3f1?source=copy_link

---
(아래는 폼에 넣지 않는 작업 메모)

## 문서 순서

`BRIEF.md` → **이 문서** → `SPEC.md` 순서로 만든다.
이 계약은 `BRIEF.md`만 근거로 하며, 아래 문서를 참조하지 않는다 — 참조는 언제나 위에서 아래 한 방향이다.
구현 명세는 `BRIEF.md`와 이 문서를 근거로 따로 쓴다.

## 되돌아갈 지점과 데이터
- 되돌리는 방법: **완료.** `C:\minyup_work\개인과제_제출`이 git 저장소이고 커밋이 둘 있다(`init: initial`, `chore: spec 예시 등록`). 원격은 `https://github.com/minyup/test`. 앞으로 Phase가 끝날 때마다 커밋을 쪼개, 어느 Phase로도 되돌아갈 수 있게 한다. **현재 문서 수정분이 아직 커밋되지 않았다 — 구현 시작 전에 한 번 커밋한다.**
- 실데이터·자격증명 없이 되게 만드는 방법 (샘플로 대체할 부분): 수집 단계 전체를 이번엔 만들지 않는다. `BRIEF.md` 7절의 `FeedbackItem`·`FeedbackAnalysis` 형식만 그대로 따르는 **가상 게시물 데이터 파일 300건**을 만들어 그것을 읽는다. 제품명·브랜드명·글 내용·URL은 전부 지어낸 값을 쓰고, 실제 사이트를 가리키지 않는 가짜 URL만 쓴다. 작성자는 비식별 값으로 고정한다. 사내 실데이터·고객명·시스템 계정·API 키는 코드와 데이터 어디에도 넣지 않는다. 요약도 샘플에 미리 채워 두어 **앱 실행에 API 키가 한 개도 필요 없게** 만든다. 네이버·Reddit·YouTube 등 키가 필요한 출처는 손대지 않고, Phase 7에서 붙이더라도 인증 없는 공개 RSS부터 본다.

---

## 고칠 곳 (4단계 반박 결과)

### 2차 재점검 (2026-08-13)

1차 지적이 대부분 해소된 상태에서 계약을 처음 보는 검토자로 다시 읽었다. **새로 보이는 것 10건.**
계약이 촘촘해진 대신 그 촘촘함이 만들어 낸 문제가 있다.

**범위**

R1. **[③ 되면 좋은] 이번엔 "되면 좋은"이 너무 크다.** 1차에서는 비어 있어서 지적했는데, 이제 Phase 5·6·7이 셋 다 들어갔다. 규칙 기반 분류(Phase 6)는 키워드 사전 + 감성 사전 + 300건 재분류 + 정확도 확인이라 그것만 2~3시간이고, RSS 수집(Phase 7)은 파서 + 중복 제거 + 스키마 매핑이라 또 2~3시간이다. 반드시 3시간에 셋을 더하면 6~8시간을 넘긴다. **어디서 멈추는지가 계약에 없다.** 셋 중 하나만 고르든, "Phase 5까지"로 선을 긋든 해야 한다.

R2. **[③↔④] Phase 6이 Phase 1~4의 완료 조건을 깨뜨린다.** ④의 기대값은 전부 Phase 1에서 고정한 건수 표에서 나온다. 그런데 Phase 6은 분류가 안 붙은 원문을 입력으로 받아 제품·카테고리·감성을 새로 매긴다. 분류기가 돌아 결과가 달라지는 순간 **Phase 1~4의 완료 조건이 전부 실패한다.** 분류 결과를 별도 파일로 내고 기존 테스트는 원래 샘플로 계속 돌린다는 식의 규칙이 필요한데, 계약에 없다.

R3. **[③↔④] Phase 7이 기준일 고정과 부딪힌다.** ④는 기준일을 고정값으로 박는다고 정했다 — 그래야 완료 조건이 며칠 뒤에 저절로 깨지지 않는다. 그런데 Phase 7에서 실제 RSS를 붙이면 **오늘 날짜의 글이 들어온다.** 기준일이 고정이면 새로 들어온 글은 "최근 30일" 밖으로 밀려나 화면에 안 보인다. 실제 데이터를 붙였는데 아무것도 안 보이는 상태가 된다.

R4. **[③] "3시간 안에 끝나면"의 판정 시점과 후퇴 규칙이 없다.** 누가 언제 시계를 보는가. 3시간이 지났는데 반드시 범위가 안 끝났으면 무엇을 버리는가. ②에 "반드시 범위가 끝난 뒤 Phase 5로 넘어갈지 판단"만 있고, **못 끝났을 때의 경로가 없다.**

**검증 불가 조건**

R5. **[④-3] "x축 눈금이 30개"는 아직 안 정한 도구의 동작에 걸려 있다.** 차트 라이브러리는 승인 지점 3번으로 **아직 미정**인데, 완료 조건이 그 라이브러리의 눈금 렌더 방식에 종속된다. 30일 축을 5일 간격 6눈금으로 솎아 그리는 것이 흔한 기본값이다. 그러면 데이터가 30일치여도 조건은 실패한다 — **틀린 이유로 실패하는 완료 조건**이다. "데이터 점 30개"처럼 데이터 기준으로 바꾸는 편이 안전하다.

R6. **[④-3] "조각이 7개"의 조각이 무엇인지 안 갈렸다.** 조각(slice)과 범례 항목은 다르다. 건수가 0인 카테고리를 조각으로 안 그리는 라이브러리면 필터에 따라 조각이 7개 미만이 된다. 조건 문장이 무엇을 세는지 명시돼 있어야 한다.

R7. **[④-1↔④-2] 앞 Phase가 뒤 Phase의 기대값을 만드는 의존이 위태롭다.** ④-2와 ④-4는 "Phase 1에서 고정한 건수 표"를 참조한다. 그 표를 Phase 1이 실제로 만들어 파일로 남기지 않으면 **Phase 2와 4는 판정 자체가 불가능하다.** ④-1의 완료 조건에 "표를 확정해 고정한다"가 들어 있긴 하지만, 그 표가 어디에 어떤 형태로 남는지(파일명·형식)가 없어 "고정했다"를 기계가 확인할 수 없다.

R8. **[④-1] "스키마 검증을 통과하고"의 검증 주체가 없다.** 어떤 스키마 파일을, 어떤 도구로(zod? JSON Schema? 직접 짠 검사기?) 돌리는지가 정해지지 않았다. 도구가 없으면 "통과"를 결국 사람이 눈으로 보게 되고, 그러면 기계 판정이 아니다.

**빠진 승인 지점**

R9. **[⑤] 생성된 샘플 데이터 300건의 내용에 승인이 없다.** 승인 지점 1번은 "도메인 값 집합"(어떤 필드, 어떤 열거형)이지 **찍어낸 글 300건 자체가 아니다.** ⑤의 "사람이 눈으로 보는 것"에 "샘플 데이터가 그럴듯한가"가 있지만 그건 검증이지 승인이 아니다. 300건을 다 만든 뒤에 "말투가 이상하다"가 나오면 전부 다시 찍어야 한다. 되돌리기가 비싼데 승인 밖에 있다.

R10. **[②↔⑤] 1차 7번과 같은 모양이 다시 생겼다.** ②의 "사람이 계속 할 일"에는 "Phase 5로 넘어갈지 판단"이 있는데, ⑤의 승인 지점 목록에는 그 항목이 없다. 승인이라고 부르는 것과 승인 지점 목록이 또 어긋난다.

> 이 재점검도 계약을 같이 쓴 사람이 하는 검토라 후하게 볼 수밖에 없다. 더 독립적인 시선을 원하면 서브에이전트에게 같은 세 가지(범위·검증 불가 조건·빠진 승인 지점)를 그대로 물어볼 수 있다 — 선택이고, 토큰을 더 쓴다.

---

### 1차 반박과 그 해소 상태

> **8건 중 7건이 해소됐다.** 아래 목록은 기록으로 남긴다.
>
> | # | 지적 | 상태 |
> |---|---|---|
> | 1 | 고른 비용과 만드는 것이 이어지지 않는다 | **부분 해소** — 수집을 Phase 7로 내려 자리를 만들었고, ①에 "이번 6~8시간이 실제로 줄이는 것: 아직 없다"를 적어 경계를 드러냈다. 범위 자체는 그대로다 |
> | 2 | "반드시"가 6~8시간의 절반이다 | **해소** — Phase 5·6·7을 ③ "되면 좋은"에 순서로 박았다 |
> | 3 | "N건 로드"의 N이 비어 있다 | **해소** — 300건 / 30일 / 제품 3개 / 카테고리 7개 |
> | 4 | "데이터 점이 0개가 아니다"는 헐겁다 | **해소** — x축 눈금 30개, 분포 차트 조각 7개 |
> | 5 | 필터 기대값이 없다 | **해소** — 조합 6가지의 정확한 건수로 판정 |
> | 6 | 기술 스택 선택에 승인이 없다 | **해소** — Next.js + JSON 확정, 차트 라이브러리는 승인 지점으로 올렸다 |
> | 7 | "승인된 레이아웃"인데 승인 지점에 없다 | **해소** — 화면 레이아웃을 승인 지점 2번으로 넣었다 |
> | 8 | 미정 항목 | **없음** |
>
> 남은 것은 1번 하나다. 1:1 코칭에서 물을 것도 결국 이 경계선이다.

**범위**

1. **[①↔③] 고른 비용과 만드는 것이 이어지지 않는다.** ①에서 줄일 비용을 "반복 — 매번 손으로 제품별 커뮤니티·뉴스를 검색한다"로 골랐는데, ③의 반드시 범위에는 수집이 없다. 가상 데이터를 읽어 그리는 대시보드는 그 검색 반복을 한 번도 줄이지 않는다. 발표 때 "그래서 주 2~3회 30분~1시간이 얼마나 줄었나"를 물으면 답할 것이 없다. ①의 비용을 바꾸든(예: 모아 놓은 것을 눈으로 훑는 "검증"), ③에 최소한의 수집을 넣든 둘 중 하나가 필요하다.

2. **[③↔④] "반드시"가 6~8시간의 절반이다.** 이미 감성·카테고리가 붙은 가상 데이터를 읽어 지표·차트·필터를 그리는 일은 본인 말대로 3시간 안팎이다. 그런데 나머지 3~5시간이 갈 "되면 좋은"에는 Phase도 "완료:" 문장도 없다. 계약에 없는 시간이 절반이라 그 시간은 즉흥으로 쓰이게 된다.

**검증 불가 조건**

3. **[④-1] "N건 로드"의 N이 비어 있다.** 숫자가 없으면 기계가 판정할 수 없고, 결국 "적당히 몇 건"이 된다. 며칠치·제품 몇 개·총 몇 건인지 숫자를 박아야 한다.

4. **[④-3] "데이터 점이 0개가 아니다"는 너무 헐겁다.** 축이 뒤바뀌어 그려져도, 날짜가 하루치만 나와도 통과한다. 사실상 "렌더는 됐다"만 판정한다.

5. **[④-4] "건수가 줄어든 값으로 바뀐다"에 기대값이 없다.** 어떤 필터를 걸면 몇 건이 되어야 하는지가 없으면, 필터가 엉뚱한 필드를 걸러도 통과한다. 샘플 데이터가 고정이므로 "제품 X + 최근 7일 = 정확히 n건"처럼 숫자를 박을 수 있다.

**빠진 승인 지점**

6. **[⑤] 기술 스택·의존성 선택에 승인이 없다.** 프레임워크·차트 라이브러리·DB를 Claude가 혼자 정하게 되어 있는데, 6~8시간 중 이걸 갈아엎으면 과제가 끝나지 않는다. 되돌리기가 가장 비싼 결정인데 승인 지점 밖에 있다.

7. **[②↔⑤] 앞뒤가 맞지 않는다.** ②에는 "승인된 레이아웃대로 화면을 구현한다"고 적혀 있는데, ⑤의 승인 지점에는 레이아웃이 없다. 승인한 적 없는 것을 "승인된"이라고 부르고 있다.

**미정으로 남은 것**

8. 없음 — 프로젝트 이름과 GitHub 저장소 URL까지 모두 채워졌다.

> 이 반박은 계약을 같이 쓴 사람이 하는 검토라 후하게 볼 수밖에 없다. 더 독립적인 시선을 원하면 서브에이전트에게 위 세 가지(범위·검증 불가 조건·빠진 승인 지점)를 그대로 물어볼 수 있다 — 선택이고, 토큰을 더 쓴다.

<!-- 기획 시작 09:24 -->
