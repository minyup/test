# 디스플레이 시장 이슈 조기 감지 대시보드

- 문서 상태: 초안 (2026-08-13 개정 — `SPEC.md`와의 충돌 해소)
- 작성일: 2026-08-12
- 우선 개발 환경: 로컬 웹 애플리케이션 — Next.js + JSON 파일 (DB 없음)
- 향후 배포 후보: Vercel

> **문서 관계** — 이 문서는 **언젠가 만들 전체 구상**을 담는다.
> 이번에 실제로 만들 1단계의 명세는 `SPEC.md`가 정본이며, 두 문서가 어긋나면 `SPEC.md`를 따른다.
> 실행 계약(시간·Phase 분할·완료 기준)은 `PLAN.md`에 있다.
>
> 아래 각 항목 뒤의 표시는 다음을 뜻한다.
>
> - **[1단계]** — 이번에 만든다 (`SPEC.md` Phase 1~4)
> - **[Phase 5]** · **[Phase 6]** · **[Phase 7]** — 명세는 `SPEC.md`에 있으나 구현은 1단계 이후
> - **[이후]** — 아직 구상 단계. 명세가 없다

## 1. 배경

모바일 및 IT 제품에 탑재되는 디스플레이를 개발하는 과정에서, 실제 시장에서 발생한 화질 문제를 개발자가 인지하기까지 시간이 오래 걸린다.

일반적으로 이슈는 다음 순서로 전달된다.

1. 소비자 커뮤니티에서 사용자 경험과 불만이 먼저 공유된다.
2. 이슈가 확산되면 뉴스나 미디어에서 다뤄진다.
3. 경영진이 관련 내용을 인지한 뒤 조사를 지시한다.
4. 개발자가 뒤늦게 원인 파악과 대응을 시작한다.

이 과정 때문에 개발자는 초기 신호를 놓치고, 이슈가 이미 커진 뒤 급하게 대응하게 된다. 따라서 여러 공개 채널의 사용자 반응을 지속적으로 수집하고, 제품별 이슈의 증가 추세를 빠르게 파악할 수 있는 도구가 필요하다.

## 2. 목표

뉴스와 주요 소비자 커뮤니티에서 제품 관련 피드백을 수집하여, 디스플레이 개발자가 시장 이슈를 기존 보고 체계보다 먼저 발견할 수 있도록 한다.

수집된 정보는 단순 목록이 아니라 제품, 이슈 카테고리, 소비자 반응, 날짜를 기준으로 구조화하고, 변화 추이를 그래프와 요약 정보로 쉽게 이해할 수 있게 제공한다.

> **1단계의 목표는 여기서 "구조화하고 보여 주는" 절반이다.** 수집은 붙이지 않는다.
> 형식만 같은 가상 샘플 데이터로 화면·집계·필터를 먼저 완성하고, 실제 출처는 Phase 7에서 그 뒤에 끼운다.
> 순서를 이렇게 잡은 이유는 수집이 외부 사이트의 약관·API 키·승인 절차에 묶여 있어,
> 그것부터 붙이면 화면을 한 번도 못 보고 시간이 끝날 수 있기 때문이다.

### 핵심 성공 기준

- 개발자가 특정 제품에서 새롭게 증가하는 부정 반응을 추세 그래프에서 발견할 수 있다. **[1단계]**
  급증을 시스템이 배지로 짚어 주는 것은 **[Phase 5]**.
- 원문 링크를 통해 결과의 근거를 바로 확인할 수 있다. **[1단계]** — 대시보드 하단 원문 목록이 담당한다.
- 제품과 감성의 날짜별 추세, 그리고 카테고리별 분포를 한 화면에서 비교할 수 있다. **[1단계]**
  카테고리는 **분포**로만 본다 — 카테고리별 추세 그래프는 **[이후]**.
- 모니터링 대상 제품을 코드 수정 없이 추가하거나 비활성화할 수 있다. **[1단계]**
  1단계에서는 관리 화면 없이 `data/products.json`을 편집하는 방식으로 충족한다. 관리 화면은 **[이후]**.
- **외부 API 키가 하나도 없어도 화면과 조회가 전부 동작한다.** **[1단계]**
  1단계는 수집 없이 가상 샘플 데이터를 읽고, 요약도 샘플에 미리 채워 둔 값을 표시만 한다.
  앱 안에서의 실제 LLM 호출은 **[이후]** — 사내 API 키 발급이 제한적이라는 제약을 전제로 한다.

## 3. 주요 사용자

### 1차 사용자

- 모바일 및 IT 제품용 디스플레이 개발자
- 화질 평가 및 품질 담당자
- 제품별 시장 품질 이슈를 추적하는 엔지니어

### 향후 사용자

- 개발 리더 및 프로젝트 매니저
- 고객 품질, 상품 기획, 경영진 보고 담당자

## 4. 핵심 사용자 시나리오

1. 사용자가 대시보드에 접속한다. **[1단계]**
2. 오늘, 최근 7일, 최근 30일 동안 제품별 언급량과 부정 반응 추이를 확인한다. **[1단계]**
   기간 선택지는 이 셋이다. 사용자 지정 기간은 **[이후]**.
3. 평소보다 언급량이나 부정 반응이 급증한 제품을 확인한다. **[Phase 5]**
   1단계에서는 추세 그래프를 사람이 읽어 판단한다.
4. 해당 제품을 선택하여 화질, 성능, 소비전력 등 카테고리별 분포를 비교한다. **[1단계]**
5. 요약과 대표 원문을 읽고 이슈의 주요 증상을 파악한다. **[1단계]**
   1단계의 요약은 샘플 데이터에 미리 채워 둔 값이며, "참고용 분석 결과"로 표시된다.
6. 원문 링크를 열어 맥락과 신뢰도를 직접 검토한다. **[1단계]**
7. 필요하면 새로운 제품명, 모델명, 별칭, 검색 키워드를 등록한다. **[1단계]**
   `data/products.json`을 직접 편집한다. 등록 화면은 **[이후]**.

## 5. 주요 기능

### 5.1 데이터 수집

> **1단계에는 수집이 없다.** 형식만 같은 가상 샘플 데이터 파일을 읽는다(`SPEC.md` 4절).
> 아래는 Phase 7 이후의 구상이다.

- 인증 없는 공개 RSS 출처 **1개** 연결 **[Phase 7]** — 첫 실제 출처는 여기서 붙인다
- 뉴스와 주요 커뮤니티의 공개 게시물 또는 검색 결과 수집 **[이후]**
- 제목, 본문 또는 요약, 작성일, 수집일, 출처, 원문 URL 저장 **[Phase 7]**
- 동일 콘텐츠의 중복 수집 방지 **[Phase 7]** — 1단계의 `content_hash` 필드가 그 자리를 잡아 둔다
- 제품별 검색 키워드와 별칭 지원 **[Phase 6]** — 분류기의 입력
- 수동 수집 **[Phase 7]** / 정기 자동 수집 **[이후]** — 스케줄링은 이번 과제 범위 밖
- 수집 실패 기록 및 재시도 상태 확인 **[이후]** — `CollectionRun` 스키마만 정의돼 있다

> 각 사이트의 이용약관, robots.txt, API 정책 및 저작권을 확인해야 한다. 공식 API나 RSS가 있는 경우 이를 우선 사용하고, 무단 크롤링을 기본 전제로 하지 않는다.

### 5.2 정보 구조화 및 분류

각 수집 항목에 다음 정보를 연결한다.

- 제품군: 스마트폰, 태블릿, 노트북, 모니터, TV
- 브랜드: 가상 브랜드만 쓴다. 실제 브랜드명은 데이터에 넣지 않는다
- 제품명 또는 모델: 가상 제품 3개 (`SPEC.md` 2.1절이 값을 확정한다)
- 이슈 카테고리: 화질, 성능, 소비전력, 가격, 내구성, 발열, 기타 — **7개 고정**
- 세부 이슈: 색 균일도, 번인, 플리커, 밝기, 색 정확도, 화면 잔상, 응답 속도, 배터리 소모, 기타
- 소비자 반응: 긍정, 부정, 중립
- 신뢰도 `confidence_score`: **1단계부터 값이 들어 있다.** 다만 화면에 쓰지는 않는다
- 키워드 및 태그

**1단계에는 분류 로직이 없다.** 위 값들이 이미 붙어 있는 가상 샘플 데이터를 읽는다.
규칙 기반 키워드 분류와 감성 분석은 **[Phase 6]** — 분류가 안 붙은 원문을 입력으로 받는다.
판단이 어려운 항목에 AI 분류를 적용하는 것은 **[이후]**.

실제 제품명·브랜드명을 쓰지 않는 이유는 사내 실데이터와 특정 회사 제품을 이 과제에 끌어들이지
않기 위해서다(`SPEC.md` 4.7절).

### 5.3 데이터 누적 및 조회

- **1단계 저장소는 `data/` 아래의 JSON 파일 세 개다. DB를 쓰지 않는다.**
  수집도 누적도 없으므로 DB가 하는 일(동시 쓰기, 중복 방지, 인덱스)이 발생하지 않는다.
  저장소를 바꿀 때 건드릴 곳은 로더 함수 하나뿐이다 (`SPEC.md` 1.1절)
- 원문과 분석 결과를 분리하여 저장 **[1단계]** — `feedback_items.json` / `feedback_analyses.json`
- 수집 데이터를 지속적으로 누적 **[Phase 7]** — 이때 SQLite 도입을 다시 검토한다
- 동일 게시물의 재수집 및 중복 저장 방지 **[Phase 7]**
- 분류 결과 수정 및 재분석 지원 **[이후]**
- 날짜, 제품별 필터 **[1단계]** / 출처, 카테고리, 감성별 필터 **[이후]**

### 5.4 대시보드 및 시각화

- 날짜별 전체 언급량 추이 **[1단계]**
- 날짜별 긍정·부정·중립 반응 추이 **[1단계]**
- 제품별 언급량 및 부정 반응 비교 **[1단계]** — 제품 필터를 바꿔 가며 비교한다
- 카테고리별 이슈 비중 **[1단계]**
- 최근 급증한 제품 또는 이슈 표시 **[Phase 5]** — 지표 카드에 배지 자리를 미리 잡아 둔다
- 대표 게시물과 관련 원문 링크 목록 **[1단계]** — 대시보드 하단
- 선택한 조건에 대한 AI 요약 **[이후]** — 1단계는 항목별로 미리 채워 둔 요약을 표시만 한다.
  조건을 받아 그때그때 생성하려면 LLM 호출이 필요하고, 그러려면 API 키가 있어야 한다

### 5.5 필터

**1단계 필터는 두 개다** — 기간(오늘 / 최근 7일 / 최근 30일)과 제품. 둘은 동시에 적용된다(AND).
이 두 개로 `MVP 범위`의 핵심 질문에 답할 수 있고, 필터를 늘릴수록 검증할 조합이 곱으로 늘어난다.

- 기간: 오늘, 최근 7일, 최근 30일 **[1단계]** / 사용자 지정 **[이후]**
- 제품 **[1단계]** / 제품군, 브랜드 **[이후]**
- 이슈 카테고리 및 세부 이슈 **[이후]**
- 소비자 반응 **[이후]**
- 출처 유형(뉴스·커뮤니티·동영상), 데이터 출처 **[이후]**

### 5.6 제품 관리

- 설정 파일(`data/products.json`)에서 제품 추가, 수정, 비활성화 **[1단계]**
- 관리 화면 **[이후]** — 1단계에는 만들지 않는다
- 제품별 공식 명칭, 모델명, 약칭, 오탈자, 한국어 키워드 등록 **[1단계]** — 필드는 있으나 화면에 안 쓰인다.
  실제로 쓰이는 것은 분류기가 붙는 **[Phase 6]**부터다. 영어 키워드는 **[이후]** — 1단계는 한국어만 다룬다
- 제외 키워드 등록 **[Phase 6]**
- 제품별 수집 대상 채널과 수집 주기 설정 **[이후]**

예시 (실제 제품이 아닌 가상 제품으로 적는다):

```text
제품군: 스마트폰
브랜드: 노바텍
제품명: 루멘 폰 X
검색 키워드: 루멘폰, 루멘 폰 X, 루멘X
제외 키워드: 케이스, 배경화면
활성 상태: 사용
```

### 5.7 AI 활용

> **1단계의 앱은 LLM을 호출하지 않는다.** 사내 API 키 발급이 제한적이라는 제약이 있어,
> 앱 실행에 키가 한 개도 필요 없게 만드는 것을 1단계의 조건으로 둔다.
> 항목별 요약(`summary`)은 가상 샘플 데이터를 만들 때 미리 채워 넣고, 화면은 그것을 표시만 한다.
> 채우는 쪽만 나중에 실제 LLM 호출로 갈아끼우면 되도록 `analysis_method` 필드가 그 자리를 표시한다.

앱 안에서 AI를 호출하게 되면 다음 작업에 쓴다.

- 긴 게시물 또는 다수 게시물의 핵심 내용 요약 **[이후]**
- 제품 및 이슈 카테고리 분류 보조 **[이후]** — 규칙 기반 분류(**[Phase 6]**)로 안 되는 것만
- 긍정·부정·중립 감성 분류 보조 **[이후]**
- 비슷한 표현의 이슈를 하나의 주제로 묶기 **[이후]**
- 특정 기간에 급증한 이슈에 대한 요약 생성 **[이후]**

분석 결과는 그것을 만든 방법과 무관하게 **항상 원문 링크와 함께 제공하며, 사실로 확정하지 않고
참고용 분석 결과임을 표시한다.** 이 규칙은 1단계의 미리 채워 둔 요약에도 그대로 적용된다.

## 6. 화면 구성 초안

> **1단계는 대시보드 한 화면뿐이다.** 아래 네 화면 중 대시보드만 만들고,
> 이슈 탐색은 대시보드 하단의 원문 목록으로 흡수한다. 화면 명세는 `SPEC.md` 5절이 정본이다.

### 대시보드 **[1단계]**

위에서 아래로 다섯 영역이다.

- 필터 바 — 기간, 제품
- 지표 카드 2개 — 언급 건수, 부정 반응 비율
  (원래 이름은 "오늘의 신규 수집 건수"였으나, 1단계에는 수집이 없어 **언급 건수**로 바꿨다)
- 날짜별 언급량·감성 추세 그래프
- 카테고리별 분포 차트
- 원문 목록 (아래 "이슈 탐색"을 흡수한 영역)

전일 또는 전주 대비 급증 이슈 배지는 **[Phase 5]** — 지표 카드에 자리만 잡아 둔다.
조건에 대한 AI 요약은 **[이후]** — 항목별 요약은 원문 목록의 각 행에 표시된다.

### 이슈 탐색 — 별도 화면은 만들지 않는다

대시보드 하단 원문 목록이 대신한다. 그 목록이 담당하는 것과 미루는 것은 다음과 같다.

- 수집 항목 목록, 제품·카테고리·감성·출처 표시, 원문 링크 이동 **[1단계]**
- 복합 필터 **[1단계]** — 다만 기간과 제품 두 개뿐이다(5.5절)
- 정렬: 최신순 **[1단계]** / 언급량순, 부정 반응순 **[이후]**

### 제품 관리 **[이후]**

1단계에는 화면이 없다. `data/products.json`을 직접 편집한다(5.6절).

- 모니터링 제품 목록, 제품 및 검색 키워드 추가·수정·비활성화, 수집 대상과 주기 설정

### 수집 상태 **[이후]**

수집이 붙는 **[Phase 7]** 이후에나 의미가 있다. 1단계에는 수집이 없어 보여 줄 상태가 없다.

- 출처별 마지막 수집 시각, 신규 수집 건수, 오류 내역 및 재시도 상태

## 7. 데이터 모델 초안

> 아래는 필드 목록만 적은 초안이다. **타입·필수 여부·값 범위가 붙은 정본은 `SPEC.md` 3절이다.**
> 1단계에 실제로 만드는 파일은 `Product` · `FeedbackItem` · `FeedbackAnalysis` 셋뿐이며,
> `Source`와 `CollectionRun`은 스키마만 두고 파일을 만들지 않는다(수집이 없으므로 채울 것이 없다).

### Product **[1단계]**

- `id`
- `category`
- `brand`
- `name`
- `aliases`
- `search_keywords`
- `exclude_keywords`
- `is_active`
- `created_at`
- `updated_at`

### Source **[Phase 7]** — 스키마만, 파일 없음

- `id`
- `name`
- `source_type`
- `base_url`
- `collection_method`
- `is_active`

### FeedbackItem **[1단계]**

- `id`
- `source_id`
- `external_id`
- `title`
- `content_excerpt`
- `original_url`
- `author_name` 또는 비식별 작성자 값
- `published_at`
- `collected_at`
- `language`
- `content_hash`

### FeedbackAnalysis **[1단계]**

- `id`
- `feedback_item_id`
- `product_id`
- `issue_category`
- `issue_subcategory`
- `sentiment`
- `summary`
- `keywords`
- `confidence_score`
- `analysis_method`
- `analyzed_at`

### CollectionRun **[이후]** — 스키마만, 파일 없음

- `id`
- `source_id`
- `started_at`
- `finished_at`
- `status`
- `items_found`
- `items_saved`
- `error_message`

## 8. 필요한 외부 API 및 서비스 후보

> **1단계는 이 표의 외부 서비스를 하나도 쓰지 않는다.** 외부 API 키가 없어도 전부 동작하는 것이
> 1단계의 조건이다. 아래는 Phase 7 이후에 붙일 후보 목록이며, 실제 사용 전 최신 요금, 호출 한도,
> 상업적 이용 조건, 데이터 저장 허용 범위를 확인해야 한다.

| 구분 | 후보 | 용도 | 필수 여부 | 확인할 사항 |
| --- | --- | --- | --- | --- |
| 국내 뉴스 검색 | 네이버 검색 API | 제품 관련 뉴스·블로그 검색 | 선택 | 검색 대상, 호출 한도, 원문 저장 범위 |
| 글로벌 뉴스 검색 | Google Programmable Search, Bing 계열 검색 API, GDELT 등 | 글로벌 뉴스와 웹 문서 탐색 | 선택 | 비용, 검색 범위, 결과 재사용 조건 |
| 커뮤니티 | Reddit API 등 공식 API | 공개 커뮤니티 게시물 및 댓글 탐색 | 선택 | 승인 절차, 호출 한도, 데이터 보관 정책 |
| 동영상 반응 | YouTube Data API | 영상 제목, 설명, 댓글 등 탐색 | 선택 | 댓글 수집 범위와 할당량 |
| 국내 커뮤니티 | 사이트별 공식 API 또는 허용된 RSS | 국내 사용자 반응 수집 | 이후 | 공식 수단 존재 여부, 크롤링 허용 여부 |
| AI 분석 | OpenAI API 또는 대체 LLM API | 요약, 분류, 감성 분석 | 선택 | 비용, 개인정보·기밀정보 전송 정책 |
| 번역 | 번역 API 또는 LLM | 해외 피드백 번역 | 선택 | 비용과 번역 품질 |
| 뉴스 피드 | 언론사 또는 사이트 RSS — 그중 **인증 없는 공개 RSS 1개**부터 | 첫 실제 출처 | **Phase 7의 시작점** | RSS 제공 여부와 본문 사용 범위 |
| 데이터 저장 | **1단계: `data/` 아래 JSON 파일 (DB 없음)** / Phase 7 이후: SQLite → PostgreSQL 계열 | 데이터 보관 | 1단계는 JSON 고정 | 바꿀 때 건드릴 곳은 로더 함수 하나 (`SPEC.md` 1.1절) |
| 스케줄링 | 로컬 작업 스케줄러, GitHub Actions, Vercel Cron 등 | 정기 수집 실행 | 이번 과제 범위 밖 | 실행 시간 제한, 비용, 비밀키 관리 |

### 사용자가 먼저 확인하거나 결정할 항목

아래 항목은 **1단계를 시작하는 데는 필요 없다.** 수집도 외부 API도 없기 때문이다.
Phase 7로 넘어가기 전에 확인한다.

- 반드시 수집해야 하는 뉴스 및 커뮤니티 목록 **(미정)**
- 각 사이트의 공식 API, RSS 또는 합법적인 데이터 이용 수단 보유 여부 **(미정)**
- 유료 API 사용 가능 여부와 월 예산 **(미정)**
- 사내 정책상 외부 API로 전송할 수 있는 데이터 범위 **(미정)**

이미 정해진 것.

- **외부 AI API 사용** — 1단계에는 쓰지 않는다. 사내 API 키 발급이 제한적이라, 앱 실행에 키가
  한 개도 필요 없게 만드는 쪽을 택했다(5.7절).
- **언어** — 1단계는 한국어만 다룬다. 영어 등 해외 콘텐츠는 이후.
- **원문 저장 범위** — 제목, 본문 발췌(60~240자), 원문 URL까지만 저장한다. 원문 전체는 저장하지 않는다.

### 현실적인 제약

**1단계에 해당하는 제약**

- **1단계는 실제 시장 이슈를 하나도 발견하지 못한다.** 가상 데이터를 읽기 때문이다.
  1단계가 증명하는 것은 "실제 데이터가 들어왔을 때 이 화면이 이슈를 보여 줄 수 있는가"까지다.
  실제 반복 업무가 줄어드는 것은 첫 실제 출처가 붙는 **[Phase 7]**부터다.
- 게시물 수만으로 실제 품질 문제의 크기나 원인을 확정할 수는 없다. 이 한계는 단계와 무관하다.

**Phase 7 이후에 해당하는 제약**

- 모든 커뮤니티를 하나의 공통 API로 수집할 수는 없다.
- 로그인이나 캡차가 필요한 사이트, 크롤링을 금지한 사이트는 자동 수집 대상에서 제외해야 할 수 있다.
- 게시물 삭제나 수정에 대비한 데이터 보관 정책이 필요하다.
- 감성 분석은 반어법, 전문 용어, 복합 평가를 잘못 분류할 수 있다.
- Vercel의 서버리스 실행 환경은 장시간 크롤링 작업에 적합하지 않을 수 있으므로, 배포 단계에서는 수집 작업을 별도 서비스로 분리할 가능성이 있다.

## 9. 개발 방향

### 1단계: 로컬 MVP — `SPEC.md` Phase 1~4

이번에 만드는 것이다. Phase별 완료 기준은 `SPEC.md` 8절에 숫자로 박혀 있다.

- 로컬 웹 애플리케이션 구축 (Next.js)
- **데이터 출처를 연결하지 않는다.** 형식만 같은 가상 샘플 데이터를 만들어 읽는다
  — 제품 3개 / 30일 / 300건 / 이슈 카테고리 7개 / 한국어
- **JSON 파일 기반 데이터 저장** (`data/` 아래 세 파일). SQLite를 쓰지 않는다
- **분류 로직 없음** — 제품·카테고리·감성이 이미 붙은 샘플을 읽는다
- 지표 카드 2개 (언급 건수, 부정 반응 비율)
- 날짜별 언급량·감성 추세 그래프, 카테고리별 분포 차트
- 기간·제품 필터 두 개
- 원문 목록과 원문 링크 제공

### 2단계: 분류와 첫 실제 출처 — `SPEC.md` Phase 5~7

1단계가 끝난 뒤 이어서 한다. 명세는 이미 `SPEC.md`에 있고 구현만 남는다.

- 급증 이슈 탐지 **[Phase 5]** — 전일·전주 대비 증가율, 지표 카드 배지
- 규칙 기반 제품·카테고리·감성 분류 **[Phase 6]** — 분류가 안 붙은 원문을 입력으로 받는다
- 인증 없는 공개 RSS 출처 1개 연결 **[Phase 7]** — 수동 실행, 중복 제거

### 2.5단계: 그 다음 **[이후]**

- 정기 수집 스케줄링, 오류 재시도 강화
- 앱 안에서의 AI 요약·분류·감성 분석 (API 키 확보가 전제)
- 제품 관리 화면, 수집 상태 화면, 이슈 탐색 별도 화면

### 3단계: 배포 및 운영

- PostgreSQL 계열 관리형 DB로 이전 검토
- Vercel을 통한 웹 화면 배포 검토
- 장시간 데이터 수집 작업의 별도 실행 환경 검토
- 인증, 권한, API 키 보안 적용
- 알림 기능 검토

## 10. MVP 범위

첫 번째 버전은 다음 질문에 답할 수 있으면 성공으로 본다.

> 최근 7일 동안 모니터링 중인 제품에서 어떤 이슈가 많이 나왔으며, 사용자 반응은 어떠하고,
> 그 근거가 되는 원문은 무엇인가?

원래 이 질문은 "어떤 이슈가 **증가했으며**"였다. 증가를 시스템이 판정해 주는 것은 **[Phase 5]**이므로,
1단계의 성공 기준은 **사람이 추세 그래프를 읽어 증가를 알아볼 수 있는 것**까지로 둔다.

### MVP 포함 (1단계)

- 가상 샘플 데이터 300건과 그것을 읽는 로더
- 원문 링크와 기본 메타데이터 저장 (제목·발췌·출처·작성일·URL)
- 지표 카드 2개 — 언급 건수, 부정 반응 비율
- 날짜별 언급량과 감성 추세 그래프, 카테고리별 분포 차트
- 기간(오늘·7일·30일)과 제품 필터
- 원문 목록과 원문 링크 이동
- 제품 등록·비활성화 — `data/products.json` 편집으로

### MVP 제외 또는 후순위

- **실제 데이터 수집 전부** — 첫 실제 출처는 **[Phase 7]**
- **제품·카테고리·감성 분류 로직** — **[Phase 6]**. 1단계 샘플에는 이미 붙어 있다
- **급증 탐지** — **[Phase 5]**
- 제품 관리 화면, 수집 상태 화면, 이슈 탐색 별도 화면
- 카테고리·감성·출처 필터, 사용자 지정 기간, 언급량순·부정 반응순 정렬
- 앱 안에서의 LLM 호출 (요약·분류·감성)
- 모든 뉴스와 커뮤니티 지원, 실시간 수집, 모바일 앱
- 자동 경영진 보고서 생성, 사용자 권한 관리
- Vercel 운영 배포, 정기 수집 스케줄링, 이메일 또는 메신저 자동 알림

## 11. 비기능 요구사항

- 원문 추적성: 모든 분석 결과에서 원문으로 이동할 수 있어야 한다. **[1단계]**
- 설명 가능성: 요약과 분류는 만든 방법과 무관하게 원문 링크 및 "참고용" 표시와 함께 낸다. **[1단계]**
- 확장성: 제품은 `products.json` 편집으로 늘린다. 출처·저장소는 인터페이스로 갈아끼운다. **[1단계]**
- 보안: **1단계는 API 키를 아예 쓰지 않는다.** 키가 생긴 뒤에는 소스 코드나 브라우저에 노출하지 않는다.
- 개인정보 보호: 불필요한 개인정보는 수집·저장하지 않는다. **[1단계]** — 작성자는 `익명N` 고정.
- 법적 준수: **1단계는 실제 사이트에 요청을 보내지 않으므로 약관·robots.txt 문제가 발생하지 않는다.**
  출처별 약관, 저작권, 데이터 보관 정책은 **[Phase 7]**에서 확인한다.
- 데이터 신뢰성: 중복 데이터와 수집 오류를 식별할 수 있어야 한다. **[Phase 7]**
  1단계는 수집이 없어 중복도 수집 오류도 발생하지 않는다.
- 수정 가능성: 잘못된 자동 분류를 사용자가 수정할 수 있어야 한다. **[이후]**
  1단계는 분류 로직 자체가 없다.

## 12. 주요 지표

- 제품별 일간 언급량 **[1단계]**
- 제품별 부정 반응 건수 및 비율 **[1단계]**
- 카테고리별 언급량 **[1단계]**
- 전일 및 전주 대비 증가율 **[Phase 5]**
- 분류되지 않은 항목 비율 **[Phase 6]** — 분류 로직이 있어야 의미가 생긴다
- 출처별 수집 성공률 **[Phase 7]**
- 신규 이슈 키워드 수 **[이후]**
- 사용자가 수정한 AI 분류 비율 **[이후]**

## 13. 향후 확장 아이디어

- 부정 반응 또는 특정 키워드 급증 시 이메일·Slack·Teams 알림
- 일간·주간 자동 요약 보고서
- 경쟁 제품 간 이슈 비교
- 지역 및 언어별 반응 비교
- 제품 출시일, 소프트웨어 업데이트일과 이슈 추세 비교
- 사내 품질 데이터와 시장 반응의 연관 분석

## 14. 오픈 이슈

### 결정된 것 (`SPEC.md`가 답을 확정했다)

1. **모니터링할 제품** — 가상 제품 3개. 실제 제품명은 쓰지 않는다 (`SPEC.md` 2.1절).
2. **이슈 카테고리와 세부 분류 체계** — 카테고리 7개 고정, 세부 이슈 9개 (`SPEC.md` 2.2절).
3. **원문 저장 범위** — 제목 + 발췌(60~240자) + URL. 원문 전체는 저장하지 않는다.
4. **해외 언어 포함 여부** — 1단계는 한국어만.
5. **저장소** — 1단계는 JSON 파일. SQLite는 Phase 7 이후 재검토.

### 아직 결정되지 않은 것

1단계를 시작하는 데는 걸림돌이 아니다. Phase 7 이후에 답이 필요하다.

1. 가장 중요한 국내외 뉴스 및 커뮤니티는 어디인가?
2. 수집 주기는 몇 시간 또는 하루 몇 회가 적절한가?
3. 유료 API와 AI API에 사용할 수 있는 월 예산은 얼마인가?
4. 사내 API 키 발급이 제한적인데 LLM 요약을 어떤 경로로 붙일 것인가?
5. 대시보드는 개인용인가, 팀 또는 사내 공유용인가?

