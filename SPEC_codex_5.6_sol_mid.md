# SPEC_codex.md 초안 — 디스플레이 시장 이슈 조기 감지 대시보드

## 1. 목적과 범위

### 1.1 목적

가상 시장 피드백 데이터를 제품·기간·이슈·감성별로 구조화해, 디스플레이 개발자가 추세와 근거 원문을 한 화면에서 검토할 수 있는 로컬 대시보드를 만든다.

이번 버전이 검증하는 것은 “실제 데이터가 주어졌을 때 이슈를 알아볼 수 있는 화면인가”까지다. 실제 검색·수집 반복을 줄이는 기능은 포함하지 않는다.

### 1.2 구현 범위

- Next.js App Router와 TypeScript 기반 로컬 웹 애플리케이션
- `data/` 아래 JSON 파일 3개 사용, DB 미사용
- 2026-07-15~2026-08-13의 가상 데이터 300건
- 기간·제품 필터
- 언급 건수와 부정 반응 비율
- 날짜별 언급량·감성 추세
- 카테고리별 분포
- 참고용 요약과 가상 원문 링크 목록
- 외부 API·LLM·인증정보 없이 실행

### 1.3 제외 범위

- 실제 뉴스·커뮤니티·동영상 데이터 수집
- 제품·이슈·감성 자동 분류
- 급증 자동 탐지
- 앱 내부 LLM 호출
- DB, 로그인, 권한, 배포, 스케줄링, 알림
- 제품 관리·수집 상태·별도 이슈 탐색 화면
- 사용자 지정 기간과 카테고리·감성·출처 필터

Phase 5~7은 후속 범위로만 기록하며 이번 상세 구현 대상에 포함하지 않는다.

## 2. 확정 도메인

### 2.1 제품

| ID | 제품군 | 브랜드 | 제품명 | 별칭 |
|---|---|---|---|---|
| `P-001` | 스마트폰 | 노바텍 | 루멘 폰 X | 루멘폰, 루멘 X, 루멘폰 엑스 |
| `P-002` | 태블릿 | 아르코 | 캔버스 탭 11 | 캔버스탭 11, 캔버스 탭, 캔버스11 |
| `P-003` | 노트북 | 벨로시스 | 클리어뷰 북 14 | 클리어뷰북 14, 클리어뷰 북, 클리어뷰14 |

모든 제품은 최초 데이터에서 활성 상태다. 비활성 제품은 제품 필터와 전체 집계에서 제외한다.

### 2.2 분류 값

이슈 카테고리는 다음 7개로 고정한다.

- 화질
- 성능
- 소비전력
- 가격
- 내구성
- 발열
- 기타

세부 이슈는 다음 9개로 고정한다.

- 색 균일도
- 번인
- 플리커
- 밝기
- 색 정확도
- 화면 잔상
- 응답 속도
- 배터리 소모
- 기타

감성은 `긍정`, `부정`, `중립` 중 하나다. 언어는 `ko`만 허용한다.

### 2.3 가상 출처

별도의 `sources.json`은 만들지 않는다. 원문 항목에 출처 정보를 포함한다.

- 출처 유형: `뉴스`, `커뮤니티`, `동영상`
- 출처명과 게시물 내용은 모두 가상 값
- URL은 `https://example.com/...` 형식만 사용
- 화면에서는 실제 원문으로 오인되지 않도록 `가상 원문`으로 표시

## 3. 데이터 계약

### 3.1 파일 구성

```text
data/
  products.json
  feedback_items.json
  feedback_analyses.json
```

애플리케이션의 다른 부분은 JSON 파일을 직접 읽지 않고 단일 로더를 통해 접근한다.

### 3.2 Product

```ts
type Product = {
  id: `P-${string}`;
  category: "스마트폰" | "태블릿" | "노트북" | "모니터" | "TV";
  brand: string;
  name: string;
  aliases: string[];
  search_keywords: string[];
  exclude_keywords: string[];
  is_active: boolean;
  created_at: string; // ISO 8601
  updated_at: string; // ISO 8601
};
```

제약:

- `id`, `brand`, `name`은 중복될 수 없다.
- 배열 값은 공백 문자열과 중복 값을 허용하지 않는다.
- 날짜는 유효한 ISO 8601 문자열이어야 한다.

### 3.3 FeedbackItem

```ts
type FeedbackItem = {
  id: `F-${string}`;
  source_id: `SRC-${string}`;
  source_name: string;
  source_type: "뉴스" | "커뮤니티" | "동영상";
  external_id: string;
  title: string;
  content_excerpt: string;
  original_url: string;
  author_name: string;
  published_at: string;
  collected_at: string;
  language: "ko";
  content_hash: string;
};
```

제약:

- 총 300건이며 `id`, `external_id`, `content_hash`는 모두 고유하다.
- `content_excerpt`는 60~240자다.
- `author_name`은 `익명N` 형식의 가상 값만 사용한다.
- `original_url`은 HTTPS `example.com` 주소만 허용한다.
- `published_at`은 기준 기간 안의 KST 시각이다.
- `collected_at`은 `published_at`과 같거나 이후다.
- `content_hash`는 소문자 16진수 SHA-256 문자열이다.

### 3.4 FeedbackAnalysis

```ts
type FeedbackAnalysis = {
  id: `A-${string}`;
  feedback_item_id: `F-${string}`;
  product_id: `P-${string}`;
  issue_category:
    | "화질"
    | "성능"
    | "소비전력"
    | "가격"
    | "내구성"
    | "발열"
    | "기타";
  issue_subcategory:
    | "색 균일도"
    | "번인"
    | "플리커"
    | "밝기"
    | "색 정확도"
    | "화면 잔상"
    | "응답 속도"
    | "배터리 소모"
    | "기타";
  sentiment: "긍정" | "부정" | "중립";
  summary: string;
  keywords: string[];
  confidence_score: number;
  analysis_method: "synthetic";
  analyzed_at: string;
};
```

제약:

- 정확히 300건이다.
- 각 원문은 분석 1건과 1:1로 대응한다.
- 존재하지 않는 `feedback_item_id`나 `product_id`를 참조할 수 없다.
- `confidence_score`는 0 이상 1 이하의 값이다.
- 요약은 화면에서 `참고용 분석 결과`로 표시한다.
- Phase 1~4에는 자동 분류나 LLM 호출이 없다.

## 4. 샘플 데이터 기준값

### 4.1 기준 기간

- 기준일: 2026-08-13, Asia/Seoul
- 오늘: 2026-08-13
- 최근 7일: 2026-08-07~2026-08-13
- 최근 30일: 2026-07-15~2026-08-13
- 모든 날짜 경계는 양 끝을 포함한다.

### 4.2 전체 분포

| 제품 | 전체 | 부정 | 긍정 | 중립 | 부정 비율 |
|---|---:|---:|---:|---:|---:|
| 전체 | 300 | 135 | 69 | 96 | 45.0% |
| P-001 | 140 | 77 | 24 | 39 | 55.0% |
| P-002 | 90 | 36 | 25 | 29 | 40.0% |
| P-003 | 70 | 22 | 20 | 28 | 31.4% |

부정 비율은 `부정 건수 ÷ 전체 언급 건수 × 100`으로 계산하고 소수점 첫째 자리에서 표시한다.

### 4.3 기간·제품 필터 기대값

| 기간 | 제품 | 언급 건수 | 부정 건수 | 부정 비율 |
|---|---|---:|---:|---:|
| 최근 30일 | 전체 | 300 | 135 | 45.0% |
| 최근 30일 | P-001 | 140 | 77 | 55.0% |
| 최근 30일 | P-002 | 90 | 36 | 40.0% |
| 최근 30일 | P-003 | 70 | 22 | 31.4% |
| 최근 7일 | 전체 | 112 | 61 | 54.5% |
| 최근 7일 | P-001 | 63 | 40 | 63.5% |
| 최근 7일 | P-002 | 29 | 12 | 41.4% |
| 최근 7일 | P-003 | 20 | 9 | 45.0% |
| 오늘 | 전체 | 18 | 11 | 61.1% |
| 오늘 | P-001 | 10 | 8 | 80.0% |
| 오늘 | P-002 | 5 | 2 | 40.0% |
| 오늘 | P-003 | 3 | 1 | 33.3% |

생성기는 같은 입력에서 항상 같은 결과가 나오도록 고정 시드를 사용한다. 각 날짜에는 최소 1건, 전체 30일·전체 제품 조건에는 모든 카테고리가 최소 1건 포함되어야 한다.

## 5. 화면과 동작

### 5.1 단일 대시보드

위에서 아래로 다음 순서를 유지한다.

1. 기간·제품 필터 바
2. 언급 건수와 부정 반응 비율 카드
3. 날짜별 언급량·감성 추세 그래프
4. 카테고리별 분포 차트
5. 원문 목록

기본 선택은 `최근 30일`과 `전체 제품`이다. 두 필터는 AND 조건으로 모든 카드·차트·목록에 동시에 적용된다.

### 5.2 지표 카드

- 언급 건수: 필터 결과의 전체 레코드 수
- 부정 반응 비율: 필터 결과 중 감성이 `부정`인 비율
- 결과가 없으면 건수는 `0`, 비율은 `0.0%`
- 급증 배지 영역은 자리만 확보하고 Phase 1~4에서는 표시하지 않는다.

### 5.3 추세 그래프

Recharts를 사용한다.

- X축: 날짜
- Y축: 건수, 0부터 시작
- 계열: 전체, 긍정, 부정, 중립
- 기본 화면에서 30개 날짜 눈금을 렌더링한다.
- 기간 필터에 따라 오늘은 1개, 최근 7일은 7개, 최근 30일은 30개 날짜를 표시한다.
- 색상뿐 아니라 범례·레이블로 계열을 구분한다.
- 마우스 오버 또는 키보드 포커스 시 날짜별 값을 확인할 수 있어야 한다.

### 5.4 카테고리 분포

Recharts 원형 차트를 사용한다.

- 필터 결과를 이슈 카테고리 7개로 집계한다.
- 기본 화면에서 7개 조각을 렌더링한다.
- 범례에는 카테고리명, 건수, 비율을 표시한다.
- 데이터가 없는 카테고리는 범례에 `0건`으로 남긴다.
- 필터 결과 전체가 0건이면 차트 대신 빈 상태를 표시한다.

### 5.5 원문 목록

최신 `published_at` 순으로 모든 필터 결과를 표시한다.

각 행에 다음을 포함한다.

- 제목
- 본문 발췌
- 제품명
- 이슈 카테고리와 세부 이슈
- 감성
- 출처명과 출처 유형
- 게시일
- 참고용 요약
- `참고용 분석 결과` 표시
- `가상 원문 열기` 링크

링크는 새 탭에서 열고 `noopener noreferrer`를 적용한다. 목록 결과가 없으면 적용된 조건과 함께 빈 상태 메시지를 표시한다.

## 6. 애플리케이션 구조와 인터페이스

- 서버 전용 로더가 세 JSON 파일을 읽고 스키마와 참조 무결성을 검증한다.
- 로더는 원문·분석·제품을 조인한 직렬화 가능한 대시보드 레코드를 반환한다.
- 페이지 서버 컴포넌트가 최초 데이터를 로드한다.
- 필터와 Recharts 영역만 클라이언트 컴포넌트로 구현한다.
- 300건 전체를 한 번 전달하고 필터·집계는 클라이언트에서 수행한다.
- API Route와 외부 네트워크 요청은 만들지 않는다.
- JSON 저장소를 교체할 때 로더 구현만 바꾸도록 UI에서 파일 경로를 직접 참조하지 않는다.

필수 명령 인터페이스:

```text
npm run generate:data  # 고정 시드로 JSON 3개 생성
npm run validate:data  # 스키마·건수·참조 무결성 검증
npm test               # 집계와 필터 단위 테스트
npm run test:e2e       # 대시보드 브라우저 검사
```

스키마 검증은 Zod, 단위 테스트는 Vitest, 브라우저 검사는 Playwright를 사용한다.

## 7. 구현 Phase와 완료 조건

### Phase 1 — 샘플 데이터와 로더

- 제품 3건, 원문 300건, 분석 300건을 생성한다.
- 모든 데이터가 스키마 검증을 통과한다.
- 원문과 분석이 누락·중복 없이 1:1로 대응한다.
- 명령 한 번으로 동일한 데이터가 재생성된다.
- 실제 회사·제품·고객·커뮤니티 이름, 사내 데이터, 자격증명은 포함하지 않는다.

### Phase 2 — 집계와 지표

- 언급 건수와 부정 반응 비율 계산을 구현한다.
- 4.2절 전체·제품별 기대값이 모두 일치한다.
- 빈 입력에서 `0건`, `0.0%`를 반환한다.
- 집계 함수는 React와 파일 시스템에 의존하지 않는 순수 함수로 작성한다.

### Phase 3 — 차트

- 기본 화면에서 추세 그래프 날짜 눈금 30개를 렌더링한다.
- 기본 화면에서 카테고리 7개가 표시된다.
- 전체·긍정·부정·중립 계열 합계가 카드 및 목록 건수와 일치한다.
- 축, 범례, 툴팁과 색상 구분을 사람이 읽을 수 있다.

### Phase 4 — 필터와 원문 목록

- 오늘·최근 7일·최근 30일 기간 필터를 제공한다.
- 전체 및 활성 제품 필터를 제공한다.
- 4.3절의 모든 조합이 정확한 건수와 비율을 반환한다.
- 필터 변경 시 카드·차트·목록이 함께 갱신된다.
- 목록은 최신순이며 각 행에서 가상 원문 링크를 열 수 있다.

## 8. 테스트 및 검수

### 자동 검사

- 잘못된 enum, 날짜, URL, 발췌 길이, 신뢰도 범위를 거부한다.
- 중복 ID·해시와 끊어진 참조를 거부한다.
- 데이터 파일별 정확한 건수를 확인한다.
- 날짜 경계가 KST 기준으로 포함되는지 확인한다.
- 전체·기간·제품별 집계값을 확인한다.
- 브라우저에서 카드 값, 차트 요소 수, 필터별 목록 건수를 확인한다.
- 외부 네트워크 요청과 환경변수 없이 페이지가 실행되는지 확인한다.

### 사람 검수

- 30일 추세와 최근 7일의 P-001 부정 반응 증가가 눈에 띄는가
- 세 출처 유형의 제목과 발췌 말투가 구분되는가
- 감성 계열과 카테고리 색상·범례가 읽기 쉬운가
- 요약과 링크가 실제 분석이나 실제 원문으로 오인되지 않는가
- 1280px 이상 데스크톱 화면에서 다섯 영역의 우선순위가 명확한가

## 9. 비기능 요구사항

- API 키와 비밀정보를 요구하거나 저장하지 않는다.
- 실제 사이트로 수집 요청을 보내지 않는다.
- 불필요한 개인정보와 실제 작성자 이름을 사용하지 않는다.
- 모든 분석 결과에 근거 항목과 참고용 표시를 제공한다.
- 데이터 로드·검증 실패 시 빈 대시보드를 조용히 표시하지 않고 원인을 명확히 보고한다.
- TypeScript 타입 검사, 단위 테스트, 브라우저 검사를 모두 통과해야 완료로 본다.

## 10. 후속 범위

Phase 1~4 완료 후 다음 순서로 별도 결정·명세한다.

1. Phase 5: 급증 탐지와 증가율 배지
2. Phase 6: 규칙 기반 제품·카테고리·감성 분류
3. Phase 7: 인증 없는 공개 RSS 출처 1개와 수동 수집·중복 제거

Phase 5의 비교 기간·임계값, Phase 6의 키워드 규칙, Phase 7의 RSS 주소와 이용 조건은 현재 초안에서 임의로 정하지 않는다.
