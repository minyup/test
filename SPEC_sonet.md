# SPEC.md 초안 작성 계획

## Context

BRIEF.md(디스플레이 시장 이슈 조기 감지 대시보드)를 기반으로 SPEC.md 초안을 작성한다. BRIEF는 이미 배경·목표·기능·데이터 모델·단계별 개발 방향까지 정리되어 있지만, 실제 구현에 들어가기 전 결정해야 할 "오픈 이슈"가 8가지 남아 있고, 그중 일부는 요구사항 간 실질적 충돌(원문 추적성 vs 저작권 준수)이나 완전한 정보 공백(기술 스택 미기재)이었다. 사용자에게 4가지 핵심 질문을 확인했고, 나머지 세부 항목(구체 제품 목록, 정확한 데이터 출처, 예산, 수집 주기 등)은 SPEC 안에서 "결정 필요" 항목으로 명시하고 임의로 확정하지 않는다.

### 확인된 결정 사항
- **문서 범위**: 1단계 로컬 MVP를 상세히 기술, 2~3단계(자동화/AI, 배포)는 "향후 확장 방향"으로 개요만 포함
- **기술 스택**: Node.js/TypeScript, Next.js 기반 (프론트+백엔드 통합, 향후 Vercel 배포 고려)
- **원문 저장 범위**: 제목 + 발췌 요약(짧은 excerpt) + 원문 링크만 저장, 전문 미저장 (BRIEF 7절 `content_excerpt` 필드와 저작권 준수 원칙에 부합)
- **언어/지역 범위**: 한국어 + 영어 해외 채널 동시 지원 (MVP 출처 1~2개 제한 안에서 국내 1곳 + 해외 1곳 조합을 우선 고려하도록 SPEC에 명시)

## SPEC.md 구조 (초안)

BRIEF.md의 정보를 재구성하여 "무엇을 만들지"가 아니라 "어떻게 검증 가능하게 구현할지"에 초점을 맞춘 문서로 작성한다.

1. **개요**: 목적, 문서 상태, BRIEF.md 대비 확정된 범위(1단계 MVP 중심) 명시
2. **범위**
   - MVP 포함/제외 (BRIEF 10절 기반, 그대로 채택)
   - 명시적으로 "SPEC 밖" 처리: 2~3단계는 별도 절에서 방향성만 요약
3. **기술 스택 및 아키텍처**
   - Next.js(TypeScript) 단일 앱, API Routes로 수집/조회 API 제공
   - SQLite (로컬 파일 DB, 예: better-sqlite3 또는 Prisma+SQLite)
   - 수집 스크립트: 공식 API/RSS 우선 (네이버 검색 API 등 국내 1곳 + 해외 RSS/공식 API 1곳 후보 예시로 제시, 최종 선정은 오픈 이슈로 남김)
   - 규칙 기반 분류기 (키워드 매칭 + 간단 감성 사전), AI 미사용
   - 시각화: 차트 라이브러리 후보 제시(예: recharts) — 선정은 구현 시 결정
4. **데이터 모델**: BRIEF 7절 스키마를 채택하되, 원문 저장 정책 결정에 맞춰 `content_excerpt`는 "최대 N자 발췌"로 명확화, 전문 필드는 두지 않음을 명시
5. **핵심 기능 명세 (MVP)**
   - 5.1 데이터 수집 (수동 트리거, 중복 방지 = content_hash 기준, 출처 1~2개: 국내+해외 각 1곳)
   - 5.2 분류 (제품/카테고리/감성 규칙 기반, 분류 실패 시 "미분류" 상태로 남기고 수동 보정 가능)
   - 5.3 조회/필터 (날짜, 제품, 카테고리, 감성 — BRIEF 5.5 중 MVP 범위만)
   - 5.4 대시보드 화면 (언급량 추이, 감성 추이, 급증 표시 — BRIEF 6절 중 MVP 해당 부분)
   - 5.5 제품 관리 (등록/수정/비활성화, 검색 키워드·제외 키워드)
6. **화면 구성**: BRIEF 6절의 대시보드/이슈 탐색/제품 관리 3개 화면을 MVP 기준으로 재정리 (수집 상태 화면은 자동 수집이 없는 MVP 특성상 최소화된 형태로 축소)
7. **비기능 요구사항**: BRIEF 11절 채택 + 원문 저장 정책 반영(전문 미저장 명시)
8. **API/외부 서비스 후보 및 확인 필요 사항**: BRIEF 8절을 MVP 범위로 축소, 확정 전 사용자 확인 필요 항목 유지
9. **향후 확장 방향 (2~3단계 요약)**: BRIEF 9절 2~3단계를 3~5줄로 요약만 제공, 상세 설계는 후속 문서로 분리
10. **결정이 필요한 오픈 이슈**: BRIEF 14절 중 이번에 답변받지 못한 항목(구체 모니터링 제품, 정확한 데이터 출처명, 수집 주기 세부값, 카테고리 세분화 수준, 예산)을 그대로 유지하고 "SPEC 승인 전 확정 필요"로 표시

## 작업 순서
1. 위 구조에 따라 `c:\minyup_work\개인과제\SPEC.md` 신규 파일 작성
2. BRIEF.md와 상충되지 않는지 재검토 (특히 데이터 모델, 필터, 화면 구성 절)
3. 사용자에게 결과 요약 제공 (반영한 결정 사항, 남겨둔 오픈 이슈 목록)

## 검증
- 코드가 아닌 문서 작업이므로 실행 테스트는 없음. 작성 후 BRIEF.md의 각 섹션(1~14절)이 SPEC.md 어딘가에 반영되었는지 대조하여 누락이 없는지 확인한다.
